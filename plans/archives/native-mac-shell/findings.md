# Findings: native-mac-shell

**日時:** 2026-07-03
**対象:** plans/native-mac-shell/

## 操作の事実
- 実機で `swift run App` を起動し、起動→編集（保存・プレビュー・ツリー操作をすべて実施）→終了までの一連の操作を手動検証した（2026-07-03）。
- 起動待ち表示（US-01シナリオ2）の見え方を確認: サーバー起動が数百ms以下と十分速く、待ち画面はほぼ視認できず瞬時に編集画面へ切り替わった。
- アプリ終了操作（ウィンドウを閉じる／プロセスをkill）を行い、サーバープロセスが残留しないことを確認。当初SIGTERMでサーバーが孤児プロセス化するバグを発見し、`DispatchSource` によるシグナルハンドラ追加で修正済み（修正後に再検証済み）。
- WKWebView移行に伴うCSSレイアウトの崩れ（範囲外としていた懸念点）の有無を確認したが、発生しなかった。

## 利用仮説の照合
- US-01（起動→編集画面表示）: 期待どおり。ただし「起動待ち表示」が視認される場面は稀（サーバー起動が速すぎるため）。
- US-02（保存・プレビュー・ツリー操作）: 期待どおり。3操作すべてを個別に確認済み。
- US-03（アプリ終了時にサーバーも終了）: 当初は外れ側（孤児プロセス化）。手動検証で発見し、修正後は期待どおりに。
- 懸念だったCSSレイアウト崩れ: 発生せず、外れなかった。

## 印象（ユーザーの言葉）
- 「特に違和感なく、想定通りの体験だった」（従来の「ターミナル起動＋ブラウザ別タブ表示」との比較）。

## 発見（解釈・原因の仮説、戻し先つき）

### F-01: 依存グラフ・ユーザーストーリーツールのSwift非対応
**戻し先:** tdd-skills
**出どころ:** observations.md
**解釈・原因の仮説:** `.claude/tdd/depgraph-regen.js` と `/tdd-userstory run`（Playwright前提）は共にJS/ESM・ブラウザ操作前提で作られており、Swift/AppKitのようなネイティブコードを含むプロジェクトでは代替手段（自作テストハーネス、手動検証）が必要だった。TDDスキル自体の考え方（できるツリー、ロード可能性チェックなど）は言語非依存だが、付属ツール群はこのリポジトリのJS前提の実装になっている。
**推奨される対応:** 複数言語混在プロジェクトでdepgraph/userstoryツールが適用範囲外になった場合の扱い（スキップの明示化、代替手段の提示）をスキル側でドキュメント化する。

### F-02: swift test（XCTest/Swift Testing）がフルXcode未導入環境で動作しない
**戻し先:** 新規plan（環境整備の要否をユーザーが判断してから）
**出どころ:** observations.md
**解釈・原因の仮説:** `Testing.framework` の実行時リンクにSIP起因の制約があり、Command Line Toolsのみの環境では標準テストランナーが使えない。自作ハーネス（`swift run CoreTests`）で代用したが、Swiftコードが増えると同じ制約が繰り返し発生する見込み。
**推奨される対応:** フルXcode導入の是非を判断する。当面は自作ハーネスの運用継続で問題ない。

### F-03: 「起動待ち表示」が実運用でほぼ視認されない
**戻し先:** クローズ（記録のみ、対応不要）
**出どころ:** 利用インタビュー
**解釈・原因の仮説:** サーバー起動が数百ms以下と十分速いため、起動待ち表示が画面に現れる時間はごくわずか。実装自体は正しく、起動が遅い環境向けのフォールバックとしての役割は今後も有効。
**推奨される対応:** 対応不要。サーバー起動が重くなる変更が入った場合に再評価する。

## まとめ
US-01/02/03すべて実機検証で期待どおりの結果が得られ（US-03は当初のバグを修正した上で）、この問題（ネイティブMacアプリ化：起動〜編集）は解決したと言える状態に達している。範囲外としていたCSSレイアウト崩れも発生しなかった。

## クローズ判断（2026-07-03）

- 問題は達成されたと判断し、この plan はクローズ（アーカイブ）する。
- F-01・F-02 はユーザー判断により「問題」として扱わず、対応不要（記録のみ）とする。
- ユーザーからは別の関心事（node同梱・sharp同梱・配布方針の検討）が新たに挙がった。これは本 plan のスコープ外（node-sea.md の既存方針の見直しに relates）であり、別途 `/tdd-problem` で新規 plan として起動することを推奨する。
