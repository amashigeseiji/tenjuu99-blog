# Observations: editor-image-library（一覧フェーズ）

**日時:** 2026-07-15

## 実装中の気づき

- **スコープを意図的に縮小した**: problem.md / user-story.md は US-01〜US-06（一覧・追加・削除・改名・公開状態導出・宣言）の6ストーリーにまたがる大きな範囲だったが、ユーザーとの相談の結果、今回のセッションは US-01（一覧・詳細確認）のみをルートXとして実装した。US-02〜US-06 は未着手のまま残っている。次回の `/tdd-run editor-image-library` は US-02（削除）から着手する想定。ルートテストと画像ライブラリ語彙は「一覧・詳細確認」の範囲に限定して green にしてあり、problem.md 全体の解決を主張するものではない。
- **`scaffold.sh` の `editor` コンテキストは `packages/editor/js/` に固定されている**が、今回追加したノード（画像スキャナー・画像メタデータ読み取り器・画像台帳・画像一覧コレクター）は fs や sharp に依存する Node 専用コードで、`packages/editor/server/` に置くのが実態に合っていた。既存の `packages/editor/server/*.js`（画像アップロードエンドポイント等）も同様に fs 依存だが scaffold の対象外だったため、今回はこれらのファイルを scaffold.sh を使わず手動で作成した。editor コンテキストが client(js/)・server(server/) の2系統を持つプロジェクト構造に scaffold.sh のマッピングが追随できていない。
- **依存グラフ（depgraph）による孤立チェックは `packages/editor/server/*.js` 全般に構造的に適用できない**: これらのファイルはディレクトリスキャン＋動的 import で読み込まれるエンドポイントのため、静的解析ベースの depgraph からは常に「依存元なし」と判定される。既存の `publish.js` / `delete.js` も同じ結果だったため、新規コードによる孤立ではなく、このプロジェクトにおける depgraph ツールの適用範囲外（動的ロードは追跡できない）と判断した。今後同様のチェックをする際は、この既知の限界を踏まえる必要がある。
- **画像詳細表示のUI統合は当初の想定より簡略化した**: ui-notes.md では「editor-options バーを含む右ペイン全体を画像詳細モードに切り替える」想定だったが、今回は `.textareaAndPreview` と `#imageDetailPanel` の表示切り替えのみとし、`.editor-options`（公開する・非公開にする・削除する等のボタン）はそのまま表示され続ける実装にした。画像詳細を見ながら誤って記事の公開・削除ボタンを押せてしまう余地が残っており、ui-notes.md が懸念していた誤操作のリスクは未解消。次フェーズ以降でUIポリッシュとして対応する必要がある。
- **受け入れテストのシナリオ4（画像0枚の空状態）は共有フィクスチャ（`src-sample/`）では検証できなかった**: 既存の画像が多数存在するため、破壊的な削除なしには「画像が1枚もない」状態を作れない。ユニットテスト（`tests/editor/image-library.test.js` のルートテスト）側で空配列ケースを検証済みのため、受け入れテストは `test.skip` として理由を明記した。
- 5回連続失敗のような詰まりポイントはなかった。葉から順に実装し、各ノードは1〜2回の実装でグリーンになった。

## 実装中の気づき（2026-07-15 / F-01・F-02 修正サイクル）

- **スコープを再度絞った**: 前回セッションのfindings.mdはF-01・F-02の解消とUS-02（削除）着手の両方を次サイクルの候補として挙げていたが、ユーザーとの相談の結果、今回はF-01・F-02の解消のみに絞った。US-02は次回に持ち越し。
- **F-01の修正方針を「再取得トリガー」から「タブを開くたびに再取得」に変更した**: 前回findings.mdは「アップロード完了時に一覧を無効化・再取得するトリガーを追加する」という設計を推奨していたが、実装時に「画像タブをクリックするたびに `initImageLibrary()` を呼び直す」という、より単純な方式を採用した。アップロード起点の専用トリガー概念を新設せずに済み、かつ将来のUS-02（削除）・US-03（改名）でも同じ仕組みで一覧が最新化されるため、前回findings.mdが懸念していた「削除・改名でも同型の問題が起きうる」への対処を先取りできた。
- **F-02の修正は`loadFileInPlace`と新規作成処理という共通の入口に`closeImageDetail()`を追加するだけで済んだ**: サイドバーリンククリック・ブラウザ戻る/進む・新規作成の3経路がすべて`loadFileInPlace`または新規作成処理を経由することが分かっていたため、個別の経路ごとに対応する必要がなかった。記事操作ボタン（公開する・非公開にする・削除する）の誤操作防止は、個々のボタンのdisabled状態を管理する既存ロジック（`fetchPublicationStatus`由来）と衝突しないよう、CSSクラストグルによる`pointer-events: none`で実現した。
- **辞書エントリの言葉選びで複数回の往復があった**: 「画像一覧表示」「画像詳細表示」の定義更新案を提示する過程で、(1) テスト所在情報（どのテストで検証されるか）を`definition`に書いてしまった、(2) 実装メカニズム（「再取得」「無効化」等の「どうやって」）を書いてしまった、(3) 画像詳細表示の定義に記事編集側の関心事（サーバーリクエストの有無、ボタン無効化、自動的に閉じる）を書いてしまった、という3段階の指摘を受けて修正した。最終的に画像詳細表示の定義は「記事編集画面とは排他的な表示関係にある」という関係性の記述のみを追加し、F-02の安全な共存という振る舞いそのものは新規語彙として登録しないことにした。
- **新規デバイス概念は登録しなかった**: 今回の修正はすべて既存関数（`initSidebarTabs`・`loadFileInPlace`・新規作成処理・`openImageDetail`・`closeImageDetail`）への配線追加であり、新しいモジュール・ファイルは作成していない。「画像一覧表示」「画像詳細表示」の定義更新のみ`plans/editor-image-library/dictionary.json`に`wip.status: "redefine"`として記録した（`add`で新規エントリとして書いてしまい、`update`で`status`を訂正する手戻りが発生した。既存の安定層エントリを再定義する際は最初から`wip.status: "redefine"`を指定すべきだった）。
- 依存グラフの孤立チェックは、新規ファイルを作成していないため対象外（スキップ）。
- **実装完了後のユーザーレビューでUI設計が組み替わった**: 最初の実装（記事操作ボタンをCSSで無効化・記事リンククリックで閉じる）に対し、ユーザーから3点の指摘を受けた: (1) 画像詳細を閉じるトリガーは「記事リンクのクリック」ではなく「画像タブを離れること（サイドバータブ切り替え）」であるべき、(2) 記事操作ボタンを無効化するのではなく、既存の記事編集ヘッダー（editor-options: 左にファイルパス・右に操作ボタン）をそのまま流用し、画像詳細表示中は左を画像パス・右を空に置き換えるべき、(3) (1)(2)を踏まえると明示的な「閉じる」ボタンは不要。この結果、`.image-detail-header`（独自のファイル名+閉じるボタン）を廃止し、`editor-options`を画像詳細表示・記事編集で共有する構造に作り直した。ツリーも3ノードから2ノードに組み換えた（詳細は test-tree.md 参照）。
- **辞書エントリの書き直しで複数回の往復があった**: 上記のUI組み換えとは別に、語彙定義の文言についても3段階の指摘（テスト所在情報を書かない／実装メカニズムを書かない／他概念の関心事を書かない）を受けた。今回はいずれもタスク完了後の設計レビューで発覚しており、実装前のツリー確認・語彙確認の段階でもっと踏み込んで質問していれば手戻りを減らせた可能性がある。次回はツリー確認時に「このノードの振る舞いを一言で言うと？」を自問し、UIの実現方法（無効化・独自ヘッダー等）を先取りしていないか確認してから実装に入るとよい。

## 実装中の気づき（2026-07-15 / US-02・US-03 削除・改名フェーズ）

- **スコープをUS-02+US-03に拡大した**: 前回セッションの見立ては「次回はUS-02（削除）のみ」だったが、参照記事の逆引き・確認ダイアログ（3択）・参照更新という共通の仕組みをUS-02（削除）・US-03（改名）の両方が必要とすることが分かっていたため、ユーザーと相談のうえ今回はまとめて設計・実装した。結果として手戻りは発生せず、この判断は妥当だった。
- **語彙の事前定義が効いた**: 「参照更新」「ローカル参照」「公開済み参照」はproblem.md v3の段階で既にapplication語彙としてdocs/dictionary.jsonに登録済みだった。今回はその実現手段（solution語彙）を登録するだけで済み、振る舞いの再定義や既存語彙との整合確認に時間を取られなかった。
- **「◯◯UI」という語彙化を一度提案し、ユーザーに却下された**: 「画像削除UI」「画像改名UI」をsolution語彙として登録しようとしたが、ユーザーから「既存の`deleteWithFeedback`・`unpublishWithFeedback`・`pullWithFeedback`（記事側の操作ボタン配線）も語彙登録されていない」という指摘を受けた。ボタン配線コードは、画像リスト表示・画像詳細表示のような実質的なレンダリング処理を持つ装置とは違い、application語彙（「削除する」等）の実装詳細にすぎない、という基準が明確になった。テストツリーのノードとしては残す（スケルトン単位としては有用）が、辞書には登録しない、という判断を採用した。次回以降、「〜UI」という名前を提案しそうになったら、対応する実質的なレンダリング処理があるかどうかを自問する。
- **「確認ダイアログ」は既存実装の追加登録として採用した**: `showConfirm`は前フェーズ以前から実装済み（記事削除確認・取り込み時の3択選択で使用）だったが、辞書未登録のままだった。今回3箇所目（画像削除・改名）の利用が生まれたタイミングで、独立した共有装置として登録した。「実装は先に存在し、語彙化は複数箇所での再利用が明確になったタイミングで行う」という順序が今回はうまく機能した。
- **`showConfirm`をモジュールトップレベルに引き上げるリファクタリングが必要になった**: 既存の`showConfirm`は`onloadFunction`というクロージャ内部に定義されており、その外側（`openImageDetail`等が定義されているモジュールトップレベル）からは参照できなかった。DOM要素の参照だけに依存し`onloadFunction`のローカル変数には依存していなかったため、安全にトップレベルへ移動できた。既存コードの一部をUI配線のために外出しする、という前フェーズまでにはなかった種類の変更だった。
- **参照の逆引き・更新は既存の画像→記事方向の抽出器をそのまま再利用できた**: `imageReferenceExtractor`・`frontmatterImageReferenceExtractor`は既に記事→画像の一方向抽出器として存在しており、これを全記事に対して回すことで画像→記事の逆引き（記事参照コレクター）を新規実装なしに構築できた。設計時点でこの再利用可能性に気づけていたため、実装時の手戻りはなかった。
- **参照更新器（frontmatterの書き換え）はYAMLリスト形式の対応が手薄**: スカラー値・JSON配列形式には対応したが、frontmatterの`- value`形式（YAMLリスト）の画像参照の書き換えは簡易的な実装にとどまり、専用のテストも書いていない。実際にそのような記法で画像を参照する記事が現れた場合は追加検証が必要。
- **手動ブラウザ確認で公開ステータスの表示に驚きがあった**: 削除確認ダイアログのスクリーンショット確認時、ローカルで新規作成しただけの記事が「（公開済み）」と表示された。これは既存の`getPublicationStatus`・`resolvePublicationMeans`（本セッションでは変更していない既存コード）が返した値をそのまま使っており、src-sample環境固有の公開手段設定に起因すると考えられる。参照記事逆引き器自体はユニットテストで新規/更新あり/公開済みを正しく区別できることを確認済みのため、本セッションのコードの不具合ではないと判断したが、次回以降 src-sample での公開ステータス確認作業をする際は留意する。
- **依存グラフの孤立チェックは今回もスキップ扱いとした**: `.claude/tdd/config.json`の`depgraph.entry_points`は`bin/*`・`lib/server/**`のみで、`packages/editor/**`を最初から含んでいない。今回新設した`packages/editor/server/*.js`はこの理由で構造的に「孤立」と判定されるため、前回セッションが記録した既知の限界（動的importで読み込まれるエンドポイント＋entry_points対象外）の範囲内と判断し、findingsには追加しなかった。
- 5回連続失敗のような詰まりポイントはなかった。葉から順に実装し、各ノードは1回の実装でグリーンになった。UI統合も既存の`editor-options`共有パターンを踏襲できたため大きな手戻りはなかった。

## 実装中の気づき（2026-07-15 / F-01〜F-04 修正サイクル）

- **語彙化の過大さをユーザーに2回訂正された**: F-01の対応として、当初「外部更新検知器」（のち「記事再読み込み要否判定」に改名）という名前で専用モジュール・単体テスト・辞書登録を用意する案を提示したが、ユーザーから「`referenceHandling === 'update'` かつ現在開いているファイルが参照記事一覧に含まれるか、という1行の条件判定に対して大げさ」と2回にわたり指摘された。1回目は辞書登録の是非（前フェーズの「画像削除UI」判断基準を援用して見送り）、2回目はモジュール・テストの分離自体の是非（ユーザーの「常にリロードでよい」という判断で判定ロジックごと不要になった）。今回の教訓を [[feedback_tdd_tree_natural_japanese]] とは別に、「1行の条件判定を独立ノードに切り出す前に、まず条件を削れないか（無条件化できないか）をユーザーに確認する」という順序にする余地がある。
- **「常にリロード」の安全性をユーザーと一緒に検討した**: 無条件リロードは「無関係な記事を開いたまま操作すると毎回リロードされる」というコストがあるが、(1) 自動保存が500msデバウンスで動いており未保存分の消失リスクはごく小さいこと、(2) 同種のリスクは既存の「取り込み後の再読み込み」処理にも既にあり許容されていること、をユーザーに説明したところ「常にリロードで構わない」との判断だった。結果、判定ロジック自体が不要になり、`editor.js`の`switchSidebarTab`と同じパターン（トップレベル`let`のプレースホルダーを`onloadFunction`内部で実体に差し替える）で`loadFileInPlace`をモジュールスコープに公開するだけで実装できた。
- **`npm run dev`（dev-server）はソースファイル変更のたびにサーバープロセスを再起動する仕様があり、手動ブラウザ確認中に接続エラーを誘発した**: 削除・改名操作自体がMarkdownファイルを書き換えるため、その書き込みが`bin/dev-server`のファイル監視に引っかかり、操作の直後にサーバーが再起動して一時的に接続エラーになった。手動確認は`npm run server`（ファイル監視のない本番相当サーバー）に切り替えることで安定した。受け入れテスト（`tests/acceptance/*.spec.ts`）の`webServer`設定はもともと`node bin/server`を使っており、かつ`gotoWithRetry`ヘルパーで接続エラーをリトライする作りになっていたため、この問題を最初から回避していた。次回、手動確認用にdev-serverを使う場合はこの点に留意する。
- **F-02（確認ダイアログ文言短縮）で受け入れテストの文言アサーションが壊れた**: `tests/acceptance/editor-image-library.spec.ts`が旧文言（「参照はそのままにして削除」等）を`hasText`でロケートしていたため、UI側の文言変更に追従して更新が必要だった。また短縮後の文言は「削除」が「削除(参照も除去)」の部分文字列になるなど、`hasText`の部分一致で複数要素にマッチしうる罠があり、`hasText: /^削除$/`のような完全一致の正規表現に直す必要があった。UIの表示文言を変更するときは、受け入れテストの文言依存ロケーターも同時に確認する習慣が必要。
- 5回連続失敗のような詰まりポイントはなかった。各修正は1〜2回の実装でグリーンになった。

## 実装中の気づき（2026-07-15 / F-05 追加サイクル）

- **findings.mdの「新たに浮かんだ要望」という解釈をユーザーに訂正された**: F-05（画像詳細に参照記事一覧を表示）は`/tdd-problem`経由が必要と当初判断したが、ユーザーから「problem.mdの表示項目列挙は例示であり閉じた許可リストではない」「既存の`画像ライブラリ`辞書エントリの関係にも一覧・詳細に参照情報を表示すると既に書かれている」と指摘された。実際に確認すると、その通り既存の関係定義に含まれていた。**解決したと言える状態の箇条書きを許可リストのように厳格に読み、軽微な追加機能まで`/tdd-problem`に差し戻すのは過剰**という教訓。次回、findings.mdや利用インタビューから出た追加要望を審査する際は、まず既存の辞書エントリの`relations`を確認し、列挙が例示か許可リストかを判断してから戻し先を決める。
- **性能面の見積もりを誤り、実装をやり直した**: 当初「一覧取得時に画像ごとの参照記事一覧をまとめて含める」設計（`get_image_library.js`に記事参照コレクター・参照記事逆引き器・公開ステータス判定器を組み込む）で実装したが、これはUS-01フェーズの利用仮説「詳細表示のたびにサーバーへ追加リクエストが飛ばない」を字面通り守ろうとした結果だった。実装後に受け入れテストのフルスイートを実行したところ、画像点数×参照記事数ぶんのgit問い合わせが一覧取得のたびに走るようになり、間欠的なタイムアウトを引き起こした。**「追加リクエストをしない」という利用仮説は、ファイルシステム由来の軽い情報（ファイル名・サイズ・解像度）を前提にしたものであり、参照情報のような重い処理には同じ前提を機械的に適用できない**、という教訓。既存の削除・改名フローが同じ重い処理を選択のたびに個別取得する設計を既に採用・許容していたので、そちらに合わせて設計をやり直した（`/get_image_references`を画像詳細選択時に個別に呼ぶ）。**「利用仮説を守る」ことと「処理の重さに応じたコスト設計をする」ことが衝突したときは後者を優先し、利用仮説側の記述を更新する**べきだった。フルスイートでの受け入れテスト実行は、単体テストだけでは見えない性能問題を検出できたという点で有効だった。
- **サーバーコードの追加が不要だった**: 最終的な設計では、`/get_image_references`エンドポイント（削除・改名フローが既に使っている）をそのまま再利用し、新規のサーバーコードは一切書かなかった。クライアント側の配線（`openImageDetail`からの個別fetch + レース条件ガード）のみで実現できた。

## 実装中の気づき（2026-07-16 / US-07 画像ナビゲーション構造統一フェーズ）

- **ツリーの組み換えは発生しなかった**: 承認された7ノードのまま、葉から順に実装して各ノード1〜2回でグリーンになった。5回連続失敗のような詰まりポイントもなし。
- **F-06（選択中ハイライト欠落）はCSSを1行も変更せずに解消した**: 画像リストを`<button>`から`<a>`（`.sidebar a`）に統一したことで、既存の`.sidebar a.active`と`.sidebar li:has(a.active)`のスタイルがそのまま画像リンクにも効くようになった。「構造を揃えれば振る舞いは共通機構から継承される」というF-07の見立てどおりの結果。
- **受け入れテストのセレクタ互換を最初から設計に含めたことで、既存の削除・改名フローのテストは無修正でパスした**: `<a>`化しても`class="image-node"`と`data-image-path`属性を維持したため、Playwrightテストのロケーターが壊れなかった。
- **統合検証中に、既存の初期化順序の問題を発見・修正した**: `DOMContentLoaded`ハンドラ内でサイドバートグルとタブのイベント配線が3つの`await fetch`の後に置かれていたため、初回描画直後（〜150ms）のクリックが「リスナー登録前」に落ちて無反応になることがあった（受け入れテストのシナリオ1がこれで2回連続失敗した）。静的要素へのイベント配線をフェッチ待ちの前に移動して解消。今回の変更で顕在化したが、構造自体は従来から存在していたレースだった。
- **`md`と`image`のURLパラメータは相互排他とし、両方が現れた場合は画像を優先する仕様を表示対象解決器のテストで固定した**: ナビゲーション時に一方をセットするとき他方を削除する配線を徹底しないとURLと表示が食い違うため、リンククリック・タブ離脱・削除・改名・新規作成の全経路で排他を配線した。「タブ離脱による画像詳細の閉じ」（F-02由来の既存挙動）もURL同期が必要になり、`leaveImageDetail`（閉じる+URLから画像の特定を取り除く）として実装した。
- **改名後のURL付け替えは`replaceState`を選んだ**: 同じ資源が新しい名前になっただけであり、履歴に旧名を残しても戻る先が存在しない（404相当）ため。削除後は記事表示への遷移として`pushState`。この使い分けは語彙化するほどの概念ではないと判断しコードコメントに留めた。
- **依存グラフの孤立チェックは今回も適用範囲外**: 新設の`packages/editor/js/displayTargetResolver.js`はグラフ上`editor.js`から参照されている（孤立ではない）が、`entry_points`（`bin/*`・`lib/server/**`）はブラウザ側エントリ（editor.html→editor.js）を表現しないため到達判定はできない。既知の限界の範囲内。
- **受け入れテスト全体を回した際、`node-modules-destruction.spec.ts`のシナリオ1が失敗したが、今回の変更を退避（git stash）しても同一に失敗する既存の失敗**（ECONNRESET、ポート8003のアプリバンドル系テスト）だった。本フェーズとは無関係。
- **docs/dictionary.json の「画像詳細表示」定義の redefine が必要**: 「記事編集画面とは排他的な表示関係にある（どちらか一方のみが表示される）」という定義は上書き表示モデルの記述であり、今回の統一で「URLで特定される対等なナビゲーション先」になった。wip辞書でのredefine登録はユーザー承認待ち（本セッション末尾で提案）。

## 実装中の気づき（2026-07-16 / US-04 画像ライブラリからの画像追加サイクル）

- **UIの担い手がツリー確定前にユーザー指摘で組み替わった**: 当初案は「画像タブに追加ボタン＋ファイル選択」だったが、ユーザーから「新規作成タブのファイル名入力はディレクトリ階層を受け付けるのに、画像追加だけベース名のみでは違和感がある。新規作成を拡張できないか」という指摘を受け、新規作成タブ拡張（ファイル選択の有無でモードが決まる）に変更した。「一覧タブに作成フォームを置かない」というタブ構成の対称性が結果として保たれ、記事と画像が同じ作成導線に乗った。経緯は test-tree.md の US-04 フェーズに記録した
- **新規ノード・新規ファイルなしの「既存装置の拡張」だけでツリーが閉じた**: 3ノードすべて既存装置（パスリゾルバー・アップロードエンドポイント・新規作成UI）の拡張で、scaffold.sh は使わなかった（新規ファイルがないため。既存ファイルへの関数追加は @vocab/@test 注釈を手動で維持）。語彙も新規登録ゼロで、安定層（docs）エントリ4件の redefine を plans/dictionary.json への add（wip付き）で行った。**promote 時に docs 側の同名エントリとのマージ（定義・relations・src の置き換え）が必要**
- **依存グラフの孤立チェックは今回も既知の限界の範囲内**: 変更したのは既存ファイル（`packages/editor/server/image_upload.js`・`packages/editor/js/editor.js`）のみ。前者は動的 import のエンドポイント、後者はブラウザ側エントリで、いずれも entry_points（bin/*・lib/server/**）から静的に到達しない既知のパターン。findings には追加しない
- **US-04 シナリオ2「追加しただけでは公開されない」の受け入れテストは US-05 に送った**: 公開・同期の検証には sync-operations.spec.ts 型の使い捨て git フィクスチャが必要で、共有フィクスチャ（src-sample）では実施できない。参照導出による公開状態は US-05 のスコープなので、その受け入れテスト整備時に同期フィクスチャごと扱うのが自然と判断し、test.skip に理由を明記した
- **統合確認は開発サーバーへの実HTTPで実施**: 追加（階層つき配置パス・sharp変換で .webp 保存）・重複409・トラバーサル400・一覧反映・追加日時記録・記事経由アップロード不変・削除エンドポイントでの後片付け（台帳整合）まで確認した
- 5回連続失敗のような詰まりポイントはなかった。リーフ2ノードはそれぞれ1回で green になった

## 実装中の気づき（2026-07-17 / US-03 移動拡張 + F-02 サイクル）

- **ツリーの組み換えは発生しなかった**: 承認された3ノード（移動先検証器・画像移動エンドポイント・画像移動UI）のまま、各ノード1回の実装で green になった。5回連続失敗のような詰まりポイントもなし
- **概念とファイルの改名を伴う拡張だった**: 問題定義 v5 の「改名は移動の特殊な場合」という再定義に合わせて、`rename_image.js`→`move_image.js`・`imageRenameUI.js`→`imageMoveUI.js` を git mv（履歴保持）で付け替えた。scaffold.sh は新規モジュールの移動先検証器のみに使用（editor コンテキストは js/ 固定のため、既知の限界どおり生成後に server/ へ移した）。既存装置の改名+拡張には scaffold を使わなかった
- **旧「改名」語彙のテストは移動語彙の describe に引き継いだ**: 旧「画像改名エンドポイント」describe のテスト5件は新 describe に統合し、旧「画像改名UI」の TODO describe 2件（削除・改名フェーズ、US-07 フェーズのURL付け替え）は新「画像移動UI」describe に統合した。統合箇所にはコメントで出自を残した。過去フェーズの describe に現れる「削除・改名」の文言も「削除・移動」へ更新した（語彙とテストの対応を保つため）
- **画像拡張子の判定基準を imageScanner から `isImageFileName` として公開して共有した**: F-02（拡張子を変えられる穴）の修正は「スキャナーが管理対象とする名前」と「移動先として受け付ける名前」が同じ述語を参照することで、基準のズレが構造的に起きなくなった
- **移動先入力の形式は US-04 の配置パス規約（image/ 相対・階層可）に揃えた**: 入力欄には現在の配置パスをプリフィルする。パス区切り入力が正当な操作になったため、F-01 で観察された「無反応に見える」混乱は入力が拒否されなくなることで自然解消した
- **docs 辞書の redefine 昇格が必要（前サイクルから累積）**: 今回 plans/dictionary.json に「画像移動エンドポイント」（新規）・「移動先検証器」（新規）・「画像ライブラリ」（redefine）を登録した。promote 時には docs 側の「画像改名エンドポイント」の退役（画像移動エンドポイントへの置き換え）もあわせて整理が必要
- **依存グラフの孤立チェックは今回も既知の限界の範囲内**: `move_image.js` は静的依存元0だが、`.cache/server` へのコピー＋動的 import で登録されるエンドポイントであり、既存の `delete_image.js` 等と同じ既知のパターン。`moveDestinationValidator.js` は `move_image.js` から静的に参照されており孤立ではない
- **受け入れテスト（US-03 セクション）はシナリオ8件に全面書き換え**: user-story.md v5 の移動シナリオ（S1〜S8。S8 が F-02 の外れ側ケース）に合わせた。63 passed / 6 skipped（既知スキップ: US-01 S4 空状態・US-04 S2 同期フィクスチャ）で pass

## 実装中の気づき（2026-07-17 / US-05 公開状態導出 + US-06 宣言 サイクル）

- **申し送りが実態とズレていた**: problem.md v5 の「/tdd-run への申し送り」は「改名の移動拡張（F-01）・拡張子の穴（F-02）は次サイクル」と書いていたが、実際にはこれは v5 と同じコミット（f0a419a）で既に実装・フィードバック済みだった（v5 の文中の「実装状況」記述が着手前のスナップショットのまま更新されていなかった）。memory もこの古い記述をそのまま反映していた。git log で実装済みを確認してから、problem.md 自身が明記する「未着手は US-05・US-06」を根拠にスコープを再確定した。**申し送りセクションは実装完了時点で実装状況に合わせて更新する運用が必要**（今回は着手側で気づいて修正したが、本来は前サイクルの終了時に更新されているべきだった）
- **US-05 と US-06 は一括実装した**: 当初の想定（別セッション）に反し、ユーザー判断で一括に変更。`画像公開状態` の GC 判定が `検出外参照宣言` を除外根拠として直接参照する設計だったため、2つを分けると宣言なしの GC を先に実装してから後で分岐を追加する手戻りが生じるところだった。一括実装により手戻りは発生しなかった
- **GCのトリガー範囲はユーザー指摘で訂正した**: 当初「公開する・更新するのみをGCトリガーにする」を recommended 案として提示したが、ユーザーから「problem.md の原則（参照を失った画像はリモートに残り続けない）と矛盾するのでは」と指摘を受けた。確認の結果、design-notes.md 自身のイベントフック一覧に「公開・更新・非公開」の3つがGC契機として明記されており、recommended 案は user-story.md 側の記述漏れを追認しただけの誤った選択肢だった。user-story.md に US-05 シナリオ5（非公開時のGC）を追加してから木の分解に進んだ。**選択肢を提示する際、一方の選択肢が「既存ドキュメントの記述漏れをそのまま追認するだけ」になっていないか、複数の一次資料（problem.md と設計ノート）を突き合わせてから提示すべきだった**
- **`RemoteState` にリモート記事内容を直接読む手段がない制約が設計を決めた**: `existsInRemote`/`diffFromRemote` はファイル単位の存在・差分のみで、「リモートの記事が今どの画像を参照しているか」は読めない。このため `公開済み参照` は design-notes.md の方針どおり、同期操作（公開・更新・非公開）のたびに増分更新する台帳フィールドとして実装する以外の選択肢がなかった
- **`unpublish.js` は `handlePublish` と非対称な構造だった**: `handlePublish` は既に `post` ハンドラから独立した関数として抽出済みでテスト可能だったが、`非公開にする` の実処理は `post` ハンドラ内にインライン実装されており単体テストの対象にできなかった（ロード可能性チェックで発覚）。`handleUnpublish` として抽出し、HTTPステータス決定（400/500の分岐）だけを `post` 側に残す形でリファクタリングした。既存の HTTP レイヤーのテストは持たなかったため、この抽出によるレイヤー変更で振る舞いが変わっていないかは手動確認とユニットテストの両方で担保した
- **既存テストが実プロジェクトの `src-sample/image-library.json` を汚染する事故が発生した**: `handlePublish`/`handleUnpublish` に `ledgerPath` のデフォルト値（本番と同じ絶対パス）を追加した結果、この既定値を上書きしていなかった既存テスト（`publish.test.js` の4件）が実行のたびに共有フィクスチャの台帳ファイルを実際に書き換えてしまった。フルスイート実行後に発覚し、該当ファイルを手動で復旧した。**新しいオプション引数にデフォルト値を追加するときは、それが本番の共有リソースを指す場合、既存のテスト（新しい引数を渡していないもの）がそのデフォルト経由で共有リソースに触れてしまわないかを必ず確認する**。該当テストは一時ディレクトリを指す `ledgerPath` を明示的に渡すよう修正した
- **U-05/U-06 の受け入れテストは既存の共有フィクスチャでは検証できず、新規の使い捨て git フィクスチャファイルが必要だった**: `editor-image-library.spec.ts` は共有の `src-sample` を対象にした dev-server 相手のテストで、実際の git 公開・非公開操作を伴う検証はできない（`publish-frontmatter-image.spec.ts`/`sync-operations.spec.ts` と同じ制約）。新規ファイル `tests/acceptance/editor-image-library-publication-state.spec.ts` として、`publish-frontmatter-image.spec.ts` と同型の使い捨てリポジトリフィクスチャ（`testInfo.parallelIndex` でポート分離）を作成した。あわせて US-04 シナリオ2（追加しただけでは公開されない）もこの新フィクスチャで検証し、`editor-image-library.spec.ts` 側の `test.skip` コメントを実態に合わせて更新した
- **依存グラフの孤立チェックは今回も既知の限界の範囲内**: 新規ファイル（`imagePublicationSyncer.js`・`image_declaration.js`）はいずれも `packages/editor/server` 配下で、`entry_points`（`bin/*`・`lib/server/**`）の対象外。`imagePublicationSyncer.js` は静的には `publish.js`/`unpublish.js` から参照されている（孤立ではない）ことをグラフで確認したが、entry_points 側の範囲外のため到達判定自体は今回も「対象外」として記録した
- **画像台帳の redefine が必要になった**: `publishedReferredBy`・`protected` フィールドの追加により、docs/dictionary.json の既存定義（追加日時のみを記録する装置、という記述）が実態と食い違った。plans/dictionary.json へ redefine として登録した（promote 時に docs 側の定義・relations を置き換える）
- 5回連続失敗のような詰まりポイントはなかった。各ノードは1〜2回の実装で green になった

## 実装中の気づき（2026-08-01 / 公開状態可視化・UI整理フェーズ、成層ツリー実験適用）

- **成層ツリー実験（役割主語root＋行為層＋能力層）は今回のスコープに合っていた**: F-01（可視化）・F-02（文言）・F-03（メタデータ欄統合）という異種の改善を1フェーズに束ねる必要があったが、行為層の中間ノード3本にそれぞれの利用仮説を割り当てたことで、find­ings粒度を保ったまま1本のツリーにまとまった。「画像詳細表示」が2つの行為層（公開状態確認・メタデータ欄統合）の共有ノードになった点も、実装上も実際に同じ関数（`showImageDetail`）の同じ変更で両方の要求を満たしたため、ツリー形状と実装が一致した
- **手順3の置き換え（受け入れテストスケルトンを赤で置く）は機能した**: root（作成者は…できる）にはロードできるモジュールがないため、代わりに US-08 の3シナリオを `TODO` アサーションで赤に置いた。F-02/F-03 は user-story.md に Given/When/Then を持たないため、スケルトンは置かず利用仮説と手動確認に委ねた。実装完了後、TODOを実アサーションに差し替えて実際に受け入れテストを実行し、rootの検証とした
- **役割語彙（domain: actor）は辞書ツールの側が未対応だった**: `dict-write.js` が `domain: "actor"` を検証エラーにしていた。ユーザーが `dict-write.js` を直接修正（`actor` domainの追加、`context: null` の要求）し、以後は正常に登録できた。実験メモが定める「新しい役割の辞書登録」は、恒久化の際にツール側の対応も合わせて反映する必要がある
- **compose手順3をユーザー指示で差し替えた**: 「root は役割主語でユニットテストが存在しないため、能力層の全ノードが緑になった時点を節目としてユーザーに確認を取り、rootの最終検証はユーザーストーリー実行（受け入れテスト）が担う」という追加指示のもとで進めた。能力層5ノード実装後に一度立ち止まってユーザー確認を取り、そのあとで受け入れテスト実行（root検証）に進む二段階の運びは、実際にF-03のDOM構造変更が既存の受け入れテスト（US-02/US-03/US-06）のセレクターに波及することが発覚するタイミングとしても機能した（早期に気づけた）
- **DOM構造変更が既存受け入れテストのセレクターに波及した**: F-03でヘッダーの独立コントロール（`#imageMoveInput`/`#imageMoveBtn`/`#imageDeclarationToggle`/`#imageDeleteBtn`/`#imageDetailOptions`）をメタデータ欄内のクラスベース要素に統合・relocateしたため、既存のUS-02（削除）・US-03（移動）・US-06（宣言）の受け入れテストのロケーターをすべて更新する必要があった。挙動そのものは変えていないため、影響はセレクターの置き換えのみに限定された。移動系操作は「編集」クリック→入力→保存の3手順に増えた（クリック一手順追加）ため、既存テストにも同じ手順を追加した
- **環境の詰まり: pnpm self-dependency（`file:./`）の古いコピーが実装変更を握りつぶしていた**: このプロジェクトは自分自身を `@tenjuu99/blog` として `file:./` 依存で `node_modules` に持ち、サーバーはHTTP経由の bare specifier 解決（`@tenjuu99/blog/lib/...`）をどこかで踏む。`node_modules/.pnpm` 配下のコピーが2026-07-16時点のまま固まっており、`pnpm install`（`--force` 含む）を実行しても再同期されなかった（ロックファイルとの整合のみを見ており、コピー内容の鮮度は見ていない模様）。このため `packages/editor/js/imageDetailDisplay.js` 等への編集が `dist/js/` に反映されず、受け入れテストが「要素が見つからない」で原因不明のタイムアウトを起こした。`node_modules/.pnpm/@tenjuu99+blog@...` と `node_modules/@tenjuu99/blog` を削除し、`node_modules/@tenjuu99/blog` をリポジトリルートへの直接シンボリックリンクに置き換えることで解消した。**この環境問題は本プランに限らずプロジェクト全体に影響するため、恒久化判断とは別に記憶しておく価値がある**
- **依存グラフ検査は専用ツール（`~/dev/tdd-skills/bin/depgraph-search.js`）を使うべきだった**: 生成済み `dependency-graph.json` を直接 `python3` で読んで確認しようとしたところ、ユーザーから専用スクリプトの存在を指摘された。専用スクリプトで確認したところ、新規・変更モジュール（`imageLibraryCollector.js`・`imageListDisplay.js`・`imageDetailDisplay.js`・`inlineFileNameEditUI.js`）はいずれも既存の呼び出し元から配線済みで孤立なし。**辞書検索と同様、依存グラフの読みも専用ツール経由を徹底する**
- 5回連続失敗のような詰まりポイントはなかった（環境問題を除く）。各ノードは1回の実装で green になった

## 分解中の気づき（2026-08-02 / US-09 リモート未達の可視化 + US-10 リモート残存の解消 + F-03/F-04 サイクル）

- **記事のサイドバーは「リモートのみ」のファイルをディレクトリツリーの中に混ぜて表示している**: `packages/editor/server/get_sidebar.js` は `collectStatuses` が返した `remote-only` のファイルを `files` 配列に push し、ローカルのファイルと同じツリーに載せている（コメント「手元に無いがリモートにあるファイルもツリーに載せる（執筆者が知覚できるように）」）。分解の対話でユーザーから「サイドバーがディレクトリ型のファイルシステムを模したものである以上、ローカルファイルシステムに無いものをその中に表示するのは直感的理解と離れる。やるのであれば別枠で表示したほうが良い」との指摘があり、画像ライブラリ側は別枠表示を採ることにした。記事サイドバー側は本プランのスコープ外だが、同じ指摘が当てはまる状態のまま残る
- **移動を「未公開＋リモートのみの2エントリ」に分裂させる案は棄却された**: 当初、公開ステータスをリモートの実体から導出すればパスの同一性がそのまま資源の同一性になる（記事側と同じモデル）として、移動後は新パス=未公開・旧パス=リモートのみ の2エントリになる案を提示した。ユーザーから「システム内の事実の表現としては正しいが、ユーザーが操作時に理解している内容は『同一のオブジェクトの属性を変化させた』でしかなく、理解からかけ離れる」との判断があり、画像台帳に移動元パスを記録して単一エントリの「更新あり」として読ませる案に切り替えた。「未公開」「リモートのみ」という状態自体が必要であることは合意されている
- **リモート一覧全体を GC の評価対象に広げる案も同時に棄却した**: 移動元パスの記録が入ったことで、US-10 S1（旧パスの除去）は「公開された画像の移動元パスを取り除く」という限定的な規則で満たせるようになった。リモートに存在する画像すべてを「公開済み参照を持たなければ除去」の対象にすると、台帳の記録より前に公開された画像や台帳を作り直した場合に、正当に公開されている画像まで消えうる（problem.md の「疑わしきは消さない」に反する）。移動由来でないリモート残存は別枠表示からの手動除去に委ねる
- **分解の手順3（受け入れテストスケルトンを赤で置く）を飛ばして合成に入りかけた**: ユーザーの指摘で気づき、その場で US-09（4シナリオ）・US-10（3シナリオ）のスケルトンを `tests/acceptance/editor-image-library-publication-state.spec.ts` に追加して赤を確認した。飛ばした要因として、`subcmds/decompose.md` の記述に次の4点が観察された。
  1. **分解の出口に手順3のゲートがない**。手順5（整形式チェック）は技術語スキャン・scaffolding feasibility・loadability の3項目、手順6（確認の拍）は語彙束縛の照合・ツリーの提示・`test-tree.md` への追記を挙げるが、どちらにも「手順3が済んでいるか」の確認項目がない。ツリーを提示して承認を得ると、手順3を通らないまま「ここで分解は終了する」に到達できる
  2. **手順3の記述が抑制方向に偏っている**。強調が3つあり、うち2つ（「シナリオの本文をここで書き込まない」「受け入れスケルトンは分解の代替ではない」）と「確認したら必ず手順4へ進む」が、いずれも留まりすぎるなという注意になっている。一方で実際に置く作業の記述（どのファイルに、どの形で置くか）は一行もない。読むと「軽く通過する場所」という圧がかかる
  3. **手順の順序が実作業と噛み合わない**。手順3は手順2（root を立てる）の直後にあるが、役割主語 root の場合この時点ではツリーが未承認である（承認は手順6）。今回は提示後の対話でツリーが2回組み替わった（「移動＝未公開＋リモートのみの2エントリ」案 → 「移動元パスによる単一エントリの更新あり」案、および「リモートのみ」の別枠表示化）ため、手順3の位置で置いていれば2回書き直していた。**後回しにする判断自体には合理性があり、その合理性が「飛ばす」と区別されないまま滑った**
  4. **実施の痕跡を残す場所が手順6のテンプレートから消えている**。前フェーズの test-tree.md には「### 手順3の置き換え（実験ルール7）」という節があり、どのファイルにスケルトンを置いたかが記録されていた。現行の手順6のテンプレートは「できるのツリー」と「利用仮説」のみで、この欄がない
  - 推奨される対応: 手順6の確認の拍に「役割主語 root の場合、該当シナリオの受け入れスケルトンが赤で存在すること」を出口ゲートとして加える。あるいは手順3を手順6の直後へ移し、役割主語 root ではツリー承認後に置くと明示する。あわせて `test-tree.md` のテンプレートにスケルトンの配置先を書く欄を戻す
  - なお、記述の問題とは別にClaude側の要因もある。分解の作業中に「受け入れスケルトンは承認後に置く」と判断しておきながら、その予定を提示メッセージにもタスクにも残さなかったため、承認後にそのまま合成へ進んだ

## 実装中の気づき（2026-08-02 / US-09 リモート未達の可視化 + US-10 リモート残存の解消 + F-03/F-04 サイクル・合成）

- **`dict-search.js` に相対パスの plans_dir を渡すと plans 側が読まれず、docs だけの結果が返る**: `plans/editor-image-library` と渡した検索が「一致するエントリなし」を返したため、分解セッションで登録済みの語彙（公開ステータスラベル・移動元パス・リモートのみ画像表示・画像除去エンドポイント、および画像台帳・画像リストコレクター・画像公開同期器・公開ステータスの redefine）を未登録と誤認し、承認の表を重複して提示した。`dict-write.js add` が「既に存在します」で拒否して初めて気づいた。絶対パス（`find-config.sh` が出す PLANS_DIR）を渡すと正しく plans 側が読まれる。他の検索（既存語彙の確認）もすべて docs だけを見ていたことになる
- **語彙規範を読む前に dict-search を呼び始めた**: ルーティングで読んだのは SKILL.md と compose.md のみで、`norms/vocabulary.md` は未読のまま辞書検索に入った。ユーザーの指摘で読んだ。compose.md は「辞書は語彙規範に従って必要に応じて検索する」と参照を促しているが、合成の入口で規範を読む手順としては現れていない
- **合成の材料化で新しく必要だったのは `src` の記録と1件の redefine だけだった**: 語彙の実質（定義・関係）は分解セッションで登録済みだったため、材料化の「solution ドメインの語彙を登録する」は、新規4件への `src` 追記と `画像ライブラリエントリ`（`published` を外し `status` を加える）の redefine に縮んだ。手順は add を前提に書かれており、分解側で登録が済んでいる場合の記述がない
- **戻り値の形の変更が過去フェーズのテストに波及した**: `collectImageLibrary` を配列から `{ images, remoteOnly }` に変えたため、フェーズ1のルートテスト2件を書き換え、`published`（二値の画像公開状態）に依存していた2つの describe（画像リストコレクター・画像リスト表示）を後続ノードへの引き継ぎとして TODO 化した。引き継ぎ元と引き継ぎ先はコメントで残した（US-03 の改名→移動と同じ扱い）
- **公開ステータスがリモート問い合わせを伴うようになり、一覧の遅延が表示のズレとして顕在化した**: 公開直後に画像タブを開くと、`initImageLibrary()` の再取得が終わる前に画像ノードのクリックが成立し、詳細パネルが取得前のエントリ（`new`）で描かれる。US-08 の受け入れテスト シナリオ1 がこれで落ちた（エンドポイント自体は `published` を返していた）。**US-08 フェーズの利用仮説が「外れ」として挙げていた「同期操作直後に再取得されない」が、そのまま現れた形**。`initImageLibrary()` の完了時に、開いている詳細を新しいエントリで描き直す（ファイル名を編集中のときは入力を捨てないよう据え置く）実装を加えて解消した
- **keep（参照を書き換えない）移動のときの GC 挙動**: 移動後の新パスのエントリは公開済み参照を引き継ぐが、記事が旧パスを参照したままだと、次の同期で新パスが「参照を失った画像」と評価され GC 対象になる。リモートにその新パスは無いため実際には no-op（`git rm --cached --ignore-unmatch`）で、公開中のサイトが参照している旧パスは残る。利用仮説の外れ側（keep なのに旧パスが消える）は避けられているが、テストの期待は「`removed` が空」ではなく「旧パスが `removed` に含まれない」に直した
- **インラインファイル名編集は Enter で確定できない**: 入力欄で Enter を押しても何も起きない（keydown 未対応。旧ヘッダー実装にも Enter 対応は無かった）。加えて画像詳細パネルは記事編集フォーム `#editor` の内側にあるため、Enter による暗黙送信が「公開する」を走らせうる位置にある
- **移動先パスの重複エラーはフッターのフィードバックにしか出ない**: 既存パスへの移動は `move_image` が拒否しメッセージを返すが、気づきにくい（メッセージの気づきにくさ自体は別プラン扱いの既知の問題）
- **F-04 の原因は意匠ではなくレイアウトだった**: 削除ボタンが横長だったのは `padding` ではなく、`.image-detail-panel` が `display: flex; flex-direction: column`（`align-items` 既定 stretch）で子が伸びていたため。`align-self: flex-start` で解消した
- **`design-token` / `ui` ドメインのエントリは辞書に1件も無い**: 意匠の完成の手順3（トンマナの適用）でトークンを検索したがヒットせず、新規の部品（リモートのみ画像表示の枠・取り除くボタン）は既存の削除ボタンと同じリテラル値を踏襲して書いた
- **依存グラフ検査**: `publicationStatusLabel.js`（被依存3）・`remoteOnlyImageDisplay.js`（被依存1）は配線済み。`remove_remote_image.js` は静的依存元0だが、`.cache/server` へのコピー＋動的 import で登録されるエンドポイントで、`delete_image.js`・`move_image.js` と同じ既知のパターン（受け入れテスト US-10 シナリオ3 が実際に `/remove_remote_image` を叩いて通っている）
- **受け入れテストの実行にブラウザの導入が必要だった**: `npx playwright test` が chromium 未導入で失敗し、`npx playwright install chromium` を実行した。導入後は全体で 81 passed / 22 skipped / 0 failed（前サイクルで記録された `node-modules-destruction.spec.ts` の失敗も再現しなかった）
- **環境の観察**: 動作確認のために `npm run dev` を起動・停止する際に `pkill -f "node bin/dev-server"` を使ったが、これは同じパターンに一致する他のプロセスも巻き込む。またポート8000は `npm run server`（ファイル監視を持たない）が保持していることがあり、その場合ソースの変更は自動では配信物に反映されない
