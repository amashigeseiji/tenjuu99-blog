# ユーザーストーリー: node-modules-destruction

**生成:** 2026-07-08
**ソース:** plans/node-modules-destruction/problem.md v1

---

## US-01: 開発中のプロジェクトを壊されずにアプリで使う

**ロール:** 開発機で既にプロジェクトを開発中の利用者（実体の `node_modules` を持つフォルダの持ち主）
**目的:** 開発中のプロジェクトをそのままネイティブアプリのコンテンツルートとして使いたい
**価値:** ローカルでの開発とアプリでの利用を、フォルダの中身を壊されることなく同居させられる

### シナリオ 1: 実体の node_modules を持つプロジェクトを選ぶ

```
Given 実体の node_modules ディレクトリを持つ開発中のプロジェクトがある
When  利用者がそのプロジェクトをアプリのコンテンツルートに選ぶ
Then  そのプロジェクト内の既存ファイル・ディレクトリは、利用者の同意なく削除・置換されない
And   アプリとしての動作（起動〜編集）が引き続き成立する
```

### シナリオ 2: 動作を成立させられない場合は黙って壊さない

```
Given 実体の node_modules ディレクトリを持つプロジェクトがある
And   その選択のままではアプリの動作を成立させられない状況である
When  利用者がそのプロジェクトをコンテンツルートに選ぶ
Then  アプリはプロジェクトの中身を黙って変更せず、利用者がその状況を認識できる
```

### シナリオ 3: 配布先での初回利用は従来どおり成立する

```
Given node_modules が存在しないコンテンツフォルダがある（配布先の Mac 相当）
When  利用者がそのフォルダをコンテンツルートに選ぶ
Then  従来どおりアプリが起動し、編集操作が行える
```
