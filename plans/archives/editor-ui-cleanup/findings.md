# Findings: editor-ui-cleanup

**日時:** 2026-06-28
**対象:** plans/editor-ui-cleanup/

---

## 操作の事実

1. 「新規作成」ボタン → 画面中央にポップアップダイアログが表示された
2. ダイアログの入力フォームで Enter キーを押す → ダイアログが閉じ、新規ファイルが作成されなかった（入力の有無にかかわらず同じ挙動）
3. 既存ファイルと同名を入力して「作成」→ ダイアログが閉じ、エディタが「新規ファイルっぽい」状態になる。リロードすると既存ファイルが読み込まれる（上書きされていない、エラーも表示されない）
4. テンプレート選択 UI はなく、ファイルパスによる自動判定が行われた
5. ファイル作成後、サイドバーに新規ファイルが表示されなかった。リロードすると表示された
6. テキスト入力後に停止 → プレビューの自動更新と自動保存が両方発生した
7. サイドバーからファイルを選んで開く操作に支障なし

---

## 利用仮説の照合

| 利用仮説 | 結果 |
|---------|------|
| 「新規作成」ボタンでファイル名を入力→確定するとテンプレート付きでエディタに開かれる | **外れ側**（Enter では確定にならない。テンプレートは自動判定のみ） |
| テキストを入力して止めると save ボタンなしで保存されている | **期待通り** |
| preview ボタンが存在せずテキスト入力でプレビューが自動更新される | **期待通り** |
| ファイル選択セレクトボックスが存在せず、サイドバーからファイルを開ける | **期待通り** |
| 新規作成したファイルが「公開済み」になる（外れ） | **外れ**（公開されていない） |
| テキスト入力後に手動 save が必要（外れ） | **外れ**（自動保存が動いた） |
| 保存時にリダイレクトされる（外れ） | **外れ**（リダイレクトなし） |

---

## 印象（ユーザーの言葉）

- ポップアップの新規作成ダイアログ: 「悪くない。もうちょっと縁が細くてもよい」
- Enter キーのキャンセル動作: 「よくない。作成と等価であってほしい」
- テンプレート自動判定: 「悪くはないが、セレクトボックスがよい。任意のファイルに任意のテンプレートを適用したい」
- 自動保存: （特に印象を述べず、動いたことを確認）
- サイドバーのファイル選択: 「操作に支障ない、良い」
- 公開ステータスのサイドバー非表示: 「どれが未公開か開いてみないとわからない。別ファイルを開いたときに見失う。色を変えるなどしたほうがよい」

---

## 発見（解釈・原因の仮説、戻し先つき）

### F-01: Enter キーがキャンセル扱いになっている

**戻し先:** /tdd-run（同一プラン）
**出どころ:** 利用インタビュー
**解釈・原因の仮説:** ダイアログの `<form>` または `<input>` の `keydown` ハンドラで Enter がデフォルト動作（フォームサブミット → ページリロード？）の防止のみ行われ、「作成」相当の処理が呼ばれていない可能性。
**推奨される対応:** 入力フォームで Enter を押したとき、入力値があれば「作成」ボタンと同じ処理を呼ぶ。

---

### F-02: 既存ファイル名での作成がサイレントに失敗する

**戻し先:** /tdd-run（同一プラン）
**出どころ:** 利用インタビュー
**解釈・原因の仮説:** 保存エンドポイント（`/save`）が既存ファイルの有無をチェックしていないか、エラーレスポンスを返しても UI 側でハンドリングしていない。ダイアログが閉じて「新規ファイル」状態になるのは、保存成功・失敗にかかわらず同じ処理をしているため。
**推奨される対応:** 新規作成時はファイルの存在確認を行い、既存の場合はダイアログにエラーを表示して作成を止める。

---

### F-03: テンプレートを選択できない（自動判定のみ）

**戻し先:** /tdd-run（同一プラン）
**出どころ:** 利用インタビュー
**解釈・原因の仮説:** problem.md「新規作成時にファイル名とテンプレートを指定できる」に対し、実装はパスによる自動判定のみ。自動判定が外れたケースはなかったが、「任意のファイルに任意のテンプレートを適用する」という自由度がない。
**推奨される対応:** ダイアログにテンプレート選択セレクトボックスを追加する。自動判定の結果をデフォルト選択として表示しつつ、上書き選択できる形が自然。

---

### F-04: 新規作成後にサイドバーが更新されない

**戻し先:** /tdd-run（同一プラン）
**出どころ:** 利用インタビュー
**解釈・原因の仮説:** 「作成」完了後にサイドバーのツリーを再取得・再レンダリングするトリガーがない。
**推奨される対応:** 新規ファイル作成成功後にサイドバーツリーを再描画する。

---

### F-05: サイドバーに公開ステータスが表示されない（新規問題）

**戻し先:** 新規プラン（/tdd-problem）
**出どころ:** 利用インタビュー（別件として言及）
**解釈・原因の仮説:** 現在のサイドバーはファイル名のみを表示し、公開ステータス（未公開・更新あり・公開済み）を反映していない。複数ファイルを行き来すると、どのファイルが未公開か・更新中かを追跡できなくなる。
**推奨される対応:** 別プランとして「サイドバーへの公開ステータス表示」を立てる。

---

### F-06: ダイアログの枠が太い（軽微）

**戻し先:** /tdd-run（同一プラン）
**出どころ:** 利用インタビュー
**解釈・原因の仮説:** ダイアログの border または box-shadow が現在のデザインに対して目立ちすぎている。
**推奨される対応:** ダイアログの縁を細くする（CSS 調整）。機能ではなくビジュアルの問題。

---

### F-07: 上部 UI のレイアウト整理

**戻し先:** /tdd-run（同一プラン）
**出どころ:** 利用インタビュー
**解釈・原因の仮説:** 現在の上部 UI は要素順・マージン・不要要素の3点で問題がある。ファイルパスは常に存在する優先度の高い情報なのに左端になく、ボタンと隣接テキストに gap がなく、不要な「戻る」リンクが残っている。
**推奨される対応:** 以下のレイアウトに変更する。

```
| ファイル名 | 更新状態(未公開|更新あり|公開済み) | [新規作成] |  （右寄せ）  | [publish] |
```

- `publish` のみ右寄せ（`justify-content: space-between` 等）
- 要素間に適切な gap を付与
- バー下部に 5px のマージンを付与
- 「戻る」リンクは削除（サイドバーがナビゲーションを代替しているため不要）

---

## クローズ判断メモ（第1ラウンド）

F-01〜F-04 および F-06 は同一プランの未達事項（problem.md の「解決したと言える状態」に対してF-01/F-03/F-04 が未達）。同一プランを /tdd-run に戻す。
F-05 はこのプランの範囲外の新規問題。アーカイブ後に新規プランを立てる。

---

# 第2ラウンド（2026-06-28、第2セッション後）

## 操作の事実

1. 既存ファイルと同名を入力して「作成」→ ダイアログにエラーが表示され、作成が止まった
2. テンプレートのセレクトボックスからテンプレートを選択できた
3. テンプレートを先に選択してからファイル名を入力 → パスマッチが走り、手動選択が解除された
4. ファイル名を先に入力してからテンプレートを選択 → 解除されず、問題なし
5. Enter キーで作成を確定 → 問題なし
6. 新規作成後にサイドバーが自動更新された
7. ダイアログの枠が消えており、見た目がきれいになった
8. 上部 UI のレイアウト整理 → 見やすく、わかりやすくなった

## 利用仮説の照合（第2ラウンド）

| 利用仮説 | 結果 |
|---------|------|
| Enter キーで作成を確定できる（F-01） | **期待通り** |
| 既存ファイル名でエラーが表示されて作成が止まる（F-02） | **期待通り** |
| テンプレートをセレクトボックスで選択できる（F-03） | **期待通り**（ただし操作順に依存する制約あり → F-08） |
| ファイル作成後にサイドバーが自動更新される（F-04） | **期待通り** |
| ダイアログの枠が細くなる（F-06） | **期待通り**（枠が消えてすっきりした） |
| 上部 UI のレイアウトが整理される（F-07） | **期待通り**（見やすくわかりやすくなった） |

## 印象（ユーザーの言葉）

- 既存ファイル名エラー表示（F-02）: 「期待どおり」
- テンプレート選択（F-03）: 「よいとおもいます。先にテンプレートを選択してからファイル名を入力すると選択が解除されてしまう、という点は不便」
- Enter キー確定（F-01）: 「問題ない」
- サイドバー自動更新（F-04）: 「問題ない」
- ダイアログ枠（F-06）: 「無くなっており、きれいになった」
- 上部 UI レイアウト（F-07）: 「かなり見易くわかりやすくなった。よい」

## 発見

### F-08: テンプレート選択の操作順依存（F-03 派生）

**戻し先:** 新規プランまたは /tdd-run（軽微）
**出どころ:** 利用インタビュー（第2ラウンド）
**操作の事実:** テンプレートを先に選択 → ファイル名を入力すると、パスマッチが走って手動選択が解除される。ファイル名を先に入力してからテンプレートを選ぶ順序なら問題ない。
**解釈・原因の仮説:** ファイル名入力時に自動パスマッチを再実行し、その結果で `<select>` の値を上書きしている。手動選択済みフラグを持たないため、手動選択後のパスマッチが上書きになる。
**推奨される対応:** ユーザーが手動でテンプレートを選んだ場合は以降の自動パスマッチを抑制する。あるいは「自動」と「手動」の状態を区別して表示する。重篤ではないため別プランでも可。

## クローズ判断（第2ラウンド）

F-01〜F-04, F-06, F-07 はすべて解決。problem.md の「解決したと言える状態」を満たしている。
F-08（テンプレート順序依存）は軽微な UX 問題。今プランの範囲外として別プランに移す。
F-05（サイドバー公開ステータス）は引き続き新規プランで対応。

**判断: C — クローズ + 新規プランへの移行**
- このプランをアーカイブする
- F-05・F-08 はそれぞれ /tdd-problem で新規プランを立てる
