# 問題定義: 画像ライブラリ

**バージョン:** v4
**最終更新:** 2026-07-16
**コンテキスト:** エディタ
**関連設計資料:** packages/editor/server/image_upload.js, packages/editor/template/editor.html, plans/editor-image-library/ui-notes.md, plans/editor-image-library/design-notes.md
**性格:** 現時点の問題理解のスナップショット。design/run/feedback を経て改版されることが正常な軌道。

## 変更履歴

### v4 (2026-07-16)
- 利用フィードバック（F-08、history/findings-2026-07-16.md）を統合: エディタのナビゲーションモデル「URLが資源を特定し、表示はURLから再構成される。編集バッファの持続を設計の前提にしない」を前提として明文化し、解決条件に1項目追加
- 上記前提により、範囲外の「編集中Markdown（未保存変更）との競合」を削除。この項目は「編集バッファが持続する」という作成者が持っていない前提から書かれていた
- 前提としていた plans/publish-frontmatter-image は解決済み（アーカイブ）となったため、関係の記述を更新
- 技術的背景と /tdd-run への申し送りを現状（US-01〜03 実装済み、語彙昇格済み、F-06/F-07 の構造統一が次サイクル冒頭）に合わせて更新

### v3 (2026-07-14)
- 画像ライブラリからの画像追加（記事編集を経由しない追加）をやりたいことに追加
- 画像の公開状態を「記事からの参照の有無」から導出するモデルを採用。画像に対する独立した公開操作は存在しない。v2の「開いている問題（公開との関係）」はこれにより解決済みとして閉じた
- 「参照」の定義を確定: 記事由来の参照（本文の `![](...)` と frontmatter の画像パス値）は自動検出し、自動検出できない参照（テンプレート等）は作成者の宣言で扱う
- 参照情報・メタデータの保持と整合性の範囲（エディタ操作の範囲で保証、外部変更は再構築で回復）を解決条件に追加
- 追加日時の表示を範囲外から範囲内に変更（エディタ経由で追加された画像に限る）
- 前提となる問題として plans/publish-frontmatter-image/problem.md を追加

### v2 (2026-07-14)
- 削除・改名時の参照の扱いを確定: 無断で参照Markdownを書き換えない。参照がある場合は参照ファイルを提示し、作成者の選択（参照はそのまま／参照も更新／中止）を経て実行する
- 一覧の対象を `src/image/` 配下と確定
- 編集中Markdown（未保存変更）との競合を範囲外に追加
- 技術的背景を更新（画像参照抽出器の存在、`image/` 保存先のエディタ規約、公開・同期操作と画像の関係）

### v1 (2026-06-26)
- 初版

---

## 問題

エディタで画像をアップロードできるようになったが、アップロード後の画像を確認・管理する手段がない。サイト全体でどんな画像が存在するか把握するにはターミナルでファイルシステムを直接参照するしかなく、非技術者にはその手段がない。また、画像を削除・改名したとき、その画像を参照しているMarkdownファイルを特定して更新する作業にも技術者の介在が必要になる。

さらに、画像の追加が記事編集（ドラッグ&ドロップ）経由に限られているため、記事と紐づかない画像の追加ができない。画像がリモートに公開されるのは記事の公開・更新に同送されるときだけであり、記事から独立して追加された画像には公開のタイミングが存在しない。画像の公開状態は、作成者が個別に操作するものではなく、記事からの参照の有無から自然に決まるべきものである。

ここで「参照」は本文の `![](...)` に限らない。frontmatter に書かれた画像パス（`og_image` 等）をテンプレートが描画する形の参照や、テンプレート自体に書かれた参照が存在しうる。テンプレートやスクリプトによる参照は静的に検出できないため、参照の自動検出は原理的に近似であり、検出できない参照を作成者が宣言できる必要がある。

### 前提: エディタのナビゲーションモデル

このエディタには、作成者が前提とするナビゲーションのモデルがある: **URLが資源を特定し、表示はURLから再構成される**。サイドバーのクリックは資源の切り替え（リンクによる遷移）であり、ファイルAからファイルBへ移れば編集バッファは当然失われる（消失は自動保存が防ぐ）。直打ち・リロードで同じ表示が再現できることが、このモデルの現れである。

画像ライブラリもこのモデルの上に載る。**編集バッファの持続を設計の前提にしない**。利用フィードバック（F-08）で、実装側が「編集中の記事」という持続する状態を前提に設計・議論しており、この食い違いが摩擦（参照更新後に古い表示が残る問題クラス、存在しない状態を守るための複雑さの検討）として繰り返し表面化していたことが確認されたため、v4 で明文化した。

### やりたいこと

- サイト全体にアップロードされた画像（`src/image/` 配下）を、エディタUIから一覧で確認したい
- 各画像のプレビュー・ファイル名・ファイルサイズ・解像度・追加日時を把握したい
- 記事編集を経由せず、画像ライブラリから画像を追加したい
- 不要な画像をエディタUIから削除したい
- 画像のファイル名を改名したい
- 削除・改名しようとした画像に参照している記事がある場合、**無断で参照を書き換えず**、実行前に参照している記事の一覧が提示されてほしい。公開済みの記事が含まれるかも識別できてほしい
- そのうえで、参照をどう扱うか（参照はそのままにする／参照も更新する／操作を中止する）を作成者自身が選べるようにしたい
- 画像の公開・非公開は、記事からの参照の有無から自動的に決まってほしい。参照されない画像が公開されず、公開済みの記事から参照が失われた画像がリモートに残り続けない状態にしたい
- 自動検出できない参照（テンプレート等）を持つ画像を宣言によって守りたい
- 上記をターミナルなしにブラウザだけで完結させたい

### 障害・制約

- 現在のエディタには画像を一覧・確認・削除・改名する手段がなく、記事編集を経由しない追加手段もない
- 画像を削除・改名したとき、参照している記事を特定して更新するには技術者の介在が必要
- 画像の参照は本文の Markdown 記法に限らず、frontmatter の値やテンプレートにも現れる。テンプレートやスクリプトによる動的な参照は静的に検出できず、参照の自動検出は原理的に近似である。検出漏れの被害は非対称で、「参照されていないのに公開する」は無害だが「参照があるのに取り除く」は公開中のサイトを壊す
- リモートの内容が変わるのは同期操作（公開・更新・非公開・取り込み）の瞬間だけである。公開状態の判断は、ローカルの編集途中の内容ではなく、公開済みの内容を根拠にしなければならない
- ライブラリから追加される画像には紐づく記事がなく、既存の保存先ルール（記事パス由来）が適用できない
- 追加日時はファイルシステムのメタデータに依存すると信頼できないため、エディタ自身が記録する必要がある

### 観察された症状

- 画像をアップロードしたが正しく保存されたか確認できない（ターミナルで `ls` するしかない）
- 不要な画像がどこにどれだけあるか把握する方法がない
- 画像を削除・改名すると、既存のMarkdownが壊れた参照を持つ可能性があるが、どのファイルが影響を受けるか分からない
- ローカルで削除した画像がリモートに孤児として残り続ける（現状、画像をリモートから取り除く手段が存在しない）

## 範囲外

- 画像の編集・変換（変換はアップロード時に行う既存機能が担う）
- `src/image/` 配下以外に手動配置された画像ファイルの管理（エディタの保存先規約の外にあるもの）
- テンプレート・スクリプトからの参照の完全な自動検出（原理的に不可能。宣言で扱う）
- エディタ導入以前から存在する画像の追加日時（記録が存在しないため表示できなくてよい）
- エディタ外での `src/image/` への直接操作に対する整合性の保証（再構築による回復のみ提供する）

## 解決したと言える状態（問題領域の言葉で）

- エディタUIから `src/image/` 配下の画像を一覧で確認できる
- 各画像のプレビュー・ファイル名・ファイルサイズ・解像度が表示され、エディタ経由で追加された画像は追加日時も確認できる
- エディタUIから、記事編集を経由せずに画像を追加できる
- エディタUIから画像を削除・改名できる
- 画像も記事と同じナビゲーションの理解で扱える: 選択した画像はURLで特定され、直打ち・リロードで同じ表示が再現され、サイドバー上で選択中の画像が識別できる
- 参照している記事がある画像を削除・改名しようとすると、実行前に参照記事の一覧が提示され、公開済みの記事が識別できる
- 参照の扱い（そのまま／更新する／中止）を作成者が選択でき、選択どおりに実行される。作成者が選択しない限りMarkdownファイルが書き換わることはない
- 画像の公開状態は記事からの参照の有無から導出される: 参照する記事が公開されれば画像も公開され、公開済みのすべての記事から参照が失われれば、同期操作のタイミングでリモートから取り除かれる。画像に対する独立した公開操作は存在しない
- 公開済みの記事がまだ参照している画像が、リモートから取り除かれることはない（疑わしきは消さない）
- 自動検出できない参照を持つ画像に作成者が宣言を付与でき、宣言された画像は参照切れ扱い・自動非公開の対象にならない
- エディタは画像ごとの参照情報・メタデータを保持し、エディタ操作の範囲では実体と整合し続ける。エディタ外の変更で崩れた場合は再構築で回復できる
- ターミナルを一切操作せずに上記すべてができる

## 他の問題との関係

- `前提とする`: plans/archives/publish-frontmatter-image/problem.md — 本問題の「参照」の定義は frontmatter からの参照を含む。公開フローの参照収集が frontmatter 参照を扱えることが先に解決されている必要があった。**解決済み（アーカイブ）となり、前提は満たされている**

## 技術的背景（調査結果）

- **画像の保存先**: `packages/editor/server/image_upload.js` の `resolveImagePath` が導出するルール（`src/image/<mdファイルのパス>/` 配下）に従い保存される。`image/` というディレクトリ名はエディタパッケージの規約としてハードコードされており、設定項目ではない。ビルド時の raw 配布対象は `blog.json` の `distribute_raw`（デフォルト `"image"`）で指定される別の仕組みで、両者は現状 `image` という名前で暗黙に一致している
- **参照の抽出**: `packages/editor/js/imageReferenceExtractor.js`（語彙: 画像参照抽出器）が本文の `![](...)` を抽出する純粋関数として存在する。frontmatter 参照（`og_image` 等）は plans/archives/publish-frontmatter-image の解決により抽出可能になった。「どの記事がこの画像を参照しているか」の逆引きは、削除・改名フェーズで参照記事一覧として実装済み
- **公開・同期操作との関係**: 画像は「公開する／更新する」時に公開対象コレクターが記事の参照から収集して同送される。つまり「参照されたら公開される」という導出モデルの追加側は実装済みで、欠けているのは除去側（参照を失った画像をリモートから取り除く）である。「非公開にする」は記事のみを対象とし、画像単体をリモートから取り除く手段は現状存在しない
- **参照形態の実例**: テンプレートは `{{OG_IMAGE}}` を描画し、値は記事の frontmatter（`og_image`）から来る。本文に `![](...)` が現れないまま記事が画像を参照するルートが既にある
- **実装状況（v4 時点）**: 一覧・詳細表示（US-01）、削除（US-02）、改名（US-03）はエディタUIから可能。参照記事の提示と作成者の選択（そのまま／更新／中止）、画像台帳（メタデータ保持）も実装済み。未着手は US-04（ライブラリからの追加）・US-05（公開状態導出）・US-06（宣言）
- **ナビゲーションの構造分岐（F-07）**: Filesタブは「`?md=` がファイルを特定し、表示はURLから再構成される」モデルだが、画像選択はURL表現を持たず `<button>` による疑似遷移で既存DOMに上書き表示する別モデルで作られた。この分岐により選択状態表示・リロード復元・戻る/進む・ディープリンクを画像側は継承できていない

## /tdd-run への申し送り

- **次サイクル冒頭（US-04 着手前）に画像ナビゲーションの構造統一（F-07）を行う**: ナビゲーションモデルの前提（v4 で明文化）を判断基準に、画像に `?md=` 相当のURL表現を与え、`<a>` ベースの共通ツリー機構に戻す。選択中画像のハイライト欠落（F-06）はこの統一で自然解消する見込み。既存の受け入れテスト（削除・改名フロー）が構造変更に耐えるかの確認も含める。詳細は history/findings-2026-07-16.md（F-06/F-07）
- 構造統一の際、docs/dictionary.json の「画像詳細表示」の定義（「記事編集画面とは排他的な表示関係にある」）は上書き表示モデルを固定化した記述のため、redefine が必要
- 削除・改名フェーズの語彙（画像ライブラリ・参照更新・ローカル参照・検出外参照宣言など）は docs/dictionary.json へ昇格済み。未着手のUS（追加・公開状態導出・宣言）で必要になる語彙（「公開済み参照」「参照導出による公開状態」等）はそのフェーズで定義する
- ライブラリからのアップロードの保存先規則（記事パス由来ルールが使えない）を設計する（US-04）
- 「参照ありだがローカルに実体なし」の縮退ケース（「参照はそのまま削除」後）: リモートは保持され、ローカルプレビューのみ壊れる。公開対象収集がローカルに無い画像を欠落エラーにしない設計が必要（US-05）
- 宣言のUI導線は設計フェーズで検討する（US-06）
- テンプレート走査による宣言候補の提案（正はあくまで宣言）は補助機能として検討してよい
- 参照情報・メタデータの保持の設計スケッチは plans/editor-image-library/design-notes.md を参照。画像台帳として一部実装済み。残るUSでの採否・構造は再検討してよい

## 参考資料

- `packages/editor/server/image_upload.js` — 現在の画像アップロード処理（保存先パスの導出ルール）
- `packages/editor/js/imageReferenceExtractor.js` — 既存の画像参照抽出（本文のみ・純粋関数）
- `packages/editor/server/publishTargetCollector.js` — 公開時に参照画像を収集する既存の仕組み
- `plans/editor-image-library/ui-notes.md` — UI検討ノート（サイドバータブ＋右ペイン詳細表示）
- `plans/editor-image-library/design-notes.md` — 設計ノート（画像メタデータ台帳の構想）
- `plans/editor-image-library/history/findings-2026-07-16.md` — 削除・改名フェーズの利用フィードバック（F-06〜F-08。F-08 を v4 に統合）
- `docs/dictionary.md` — エディタコンテキストの既存語彙
