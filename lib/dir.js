const rootDir = process.cwd()
const srcDir = `${rootDir}/data`
const distDir = `${rootDir}/dist`
const templateDir = `${rootDir}/template`
const cacheDir = `${rootDir}/.cache`

export {
  rootDir,
  srcDir,
  distDir,
  templateDir,
  cacheDir,
}
